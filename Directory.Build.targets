<Project>
  <Target Name="_SetVSTestLogger" BeforeTargets="VSTest" Condition="'$(CI)' == 'true'">
  <PropertyGroup>
    <VSTestLogger>trx%3BLogFileName=$(AssemblyName)-$(TargetFramework).trx</VSTestLogger>
  </PropertyGroup>
  </Target>
</Project>
